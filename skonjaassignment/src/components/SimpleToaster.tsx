import React from 'react';

// Simple Toaster component - just a placeholder that doesn't break the app
const Toaster: React.FC = () => {
  return null; // This is a no-op component to prevent breaking the app
};

// Simple Sonner component - just a placeholder that doesn't break the app
const Sonner: React.FC = () => {
  return null; // This is a no-op component to prevent breaking the app
};

export { Toaster, Sonner }; 